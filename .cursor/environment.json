{
  "install": "corepack enable && corepack pnpm install --frozen-lockfile || corepack pnpm install",
  "start": "mkdir -p \"$HOME/.cursor/skills\" && if [ -d .cursor/skills ]; then cp -R .cursor/skills/. \"$HOME/.cursor/skills/\"; fi"
}
